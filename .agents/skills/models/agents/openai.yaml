interface:
  display_name: "Models"
  short_description: "Repo-specific patterns and workflows for models"
  default_prompt: "Use the models repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true